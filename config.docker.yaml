# Docker path overrides — mounted at runtime with instance config (see docker-compose.yml).
# Production instances bind only config.yaml; local compose layers this file on top.
api:
  data_dir: /app/data

crawler:
  state_db: /app/.myskin/crawl.db
