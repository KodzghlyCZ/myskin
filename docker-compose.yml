services:
  myskin:
    build: .
    image: myskin:latest
    container_name: myskin
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      MYSKIN_CONFIG_FILES: /app/config.yaml,/app/config.docker.yaml
    volumes:
      - myskin-data:/app/data
      - myskin-state:/app/.myskin
      - ./config.yaml:/app/config.yaml:ro
      - ./config.docker.yaml:/app/config.docker.yaml:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  myskin-data:
  myskin-state:
