# Secrets only — copy to .env and keep out of version control.
MYSKIN_API_TOKEN=change-me
MYSKIN_RAGFLOW_API_KEY=

# Required when auth.enabled / OIDC is on in config.yaml:
# SESSION_SECRET=change-me-use-openssl-rand-hex-32
# OIDC_CLIENT_SECRET=from-keycloak-client-secret
